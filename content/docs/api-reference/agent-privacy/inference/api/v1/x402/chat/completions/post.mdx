---
title: Cryptographically private AI inference (Arcium-encrypted, x402-paywalled)
description: >-
  OpenAI-compatible chat completions. The prompt MUST be Arcium-encrypted
  client-side using `@solrouter/sdk`'s `encryptPrompt()` helper. The Solrouter
  backend relays the ciphertext without reading it; the enclave decrypts it and
  calls a Nosana GPU node, which sees the prompt and reply in plaintext during
  inference. The response returns encrypted. Pay-per-call x402 ($0.02 USDC flat
  per call). A signed-in caller with an active plan spends one plan call
  instead.
full: true
_openapi:
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          OpenAI-compatible chat completions. The prompt MUST be
          Arcium-encrypted client-side using `@solrouter/sdk`'s
          `encryptPrompt()` helper. The Solrouter backend relays the ciphertext
          without reading it; the enclave decrypts it and calls a Nosana GPU
          node, which sees the prompt and reply in plaintext during inference.
          The response returns encrypted. Pay-per-call x402 ($0.02 USDC flat per
          call). A signed-in caller with an active plan spends one plan call
          instead.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

<APIPage document={"./openapi/agent-privacy.json"} operations={[{"path":"/api/v1/x402/chat/completions","method":"post"}]} />